# Database
DATABASE_URL=

# Auth (generate with: openssl rand -base64 32)
AUTH_SECRET=

# Create the single admin account once at /admin/login after deploy.
# OpenAI (required for AI verification verdicts)
OPENAI_API_KEY=

# News & search APIs
NEWS_API_KEY=
GNEWS_API_KEY=
BING_SEARCH_API_KEY=
TAVILY_API_KEY=

# Optional: Cloudinary for team photo uploads (cloudinary://key:secret@cloud_name)
CLOUDINARY_URL=
